#!/usr/bin/env bash
# Display PID and full executable path of processes with 'bash' in their name
for pid in $(pgrep -f bash); do
if [ -e /proc/$pid/exe ]; then
exe_path=$(readlink -f /proc/$pid/exe)
echo "$pid $exe_path"
fi
done
